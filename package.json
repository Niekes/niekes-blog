{
  "name": "themes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gulp clean && deploy && gulp build && deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp-clean": "^0.3.2",
    "gulp-livereload": "^3.8.1"
  }
}
